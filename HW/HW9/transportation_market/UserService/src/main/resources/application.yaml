server:
  port: 8081

spring:
  application:
    name: UserService
  datasource:
    url: jdbc:postgresql://localhost:5432/jpatest
    username: sa
    password: 123
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
